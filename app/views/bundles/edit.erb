<div class="container m-b">
  <%= render partial: 'form', locals: { action: song_path(@bundle) } %>
</div>
   
<div class="container">
  <%= form_for @bundle, url: song_path(@bundle), method: :delete do |f| %>
    <div class="actions">
      <%= f.submit "Delete This Map", class: 'btn waves-effect waves-light' %>
    </div>
  <% end %>
</div>
